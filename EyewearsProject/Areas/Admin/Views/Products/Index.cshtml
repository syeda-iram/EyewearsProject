@using EyewearsProject.Extensions
@model List<EyewearsProject.Models.Product>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4>Products</h4>

    <a asp-action="Create"
       class="btn btn-dark">
        + New Product
    </a>
</div>

<div class="table-responsive">
    <table class="table table-bordered bg-white align-middle">
        <thead>
            <tr>
                <th>Name</th>
                <th>SKU</th>
                <th>Category</th>
                <th>Subcategory</th>
                <th>Brand</th>
                <th>Gender</th>
                <th>Material</th>
                <th>Shape</th>
                <th>Price</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var p in Model)
                {
                    <tr>
                        <td>
                            <strong>@p.Name</strong>
                        </td>

                        <td>
                            @p.Sku
                        </td>

                        <td>
                            @(p.Category?.Name ?? "-")
                        </td>

                        <td>
                            @(p.SubCategory?.Name ?? "-")
                        </td>

                        <td>
                            @(p.Brand?.Name ?? "Unbranded")
                        </td>

                        <td>
                            @(string.IsNullOrWhiteSpace(p.Gender)
                                ? "-"
                                : p.Gender)
                        </td>

                        <td>
                            @(string.IsNullOrWhiteSpace(p.Material)
                                ? "-"
                                : p.Material)
                        </td>

                        <td>
                            @(string.IsNullOrWhiteSpace(p.Shape)
                                ? "-"
                                : p.Shape)
                        </td>

                        <td>
                            @if (p.DiscountPrice.HasValue &&
                                 p.DiscountPrice.Value < p.Price)
                            {
                                <div>
                                    <span class="text-decoration-line-through text-muted">
                                        @p.Price.ToPkr()
                                    </span>
                                </div>

                                <strong class="text-danger">
                                    @p.DiscountPrice.Value.ToPkr()
                                </strong>
                            }
                            else
                            {
                                @p.Price.ToPkr()
                            }
                        </td>

                        <td>
                            @if (p.IsFeatured)
                            {
                                <span class="badge bg-warning text-dark mb-1">
                                    Featured
                                </span>

                                <br />
                            }

                            @if (p.IsActive)
                            {
                                <span class="badge bg-success">
                                    Active
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">
                                    Inactive
                                </span>
                            }
                        </td>

                        <td>
                            <div class="d-flex flex-wrap align-items-center" style="gap:6px;">
                                <!-- EDIT -->
                                <a asp-action="Edit"
                                   asp-route-id="@p.Id"
                                   class="btn btn-sm btn-outline-primary btn-icon-circle"
                                   title="Edit Product">
                                    <i class="bi bi-pencil"></i>
                                </a>

                                <!-- ACTIVATE / DEACTIVATE -->
                                <form asp-action="ToggleActive"
                                      asp-route-id="@p.Id"
                                      method="post"
                                      class="d-inline">

                                    @Html.AntiForgeryToken()

                                    <label class="toggle-switch" title="@(p.IsActive ? "Deactivate" : "Activate")">
                                        <input type="checkbox"
                                               checked="@(p.IsActive ? "checked" : null)"
                                               onchange="this.form.submit()" />
                                        <span class="toggle-slider"></span>
                                    </label>
                                </form>

                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="11"
                        class="text-center text-muted py-4">
                        No products found.
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>